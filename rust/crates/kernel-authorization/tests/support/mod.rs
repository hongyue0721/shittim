//! Shared fixtures and assertion helpers for kernel-authorization integration tests.
//!
//! Each integration test binary only uses a subset of helpers, so unused items are expected.
#![allow(dead_code)]

pub mod assertions;
pub mod fixtures;
